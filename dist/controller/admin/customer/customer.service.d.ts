import { Repository } from 'typeorm';
import { User } from 'src/schema/user.schema';
import { Rating } from 'src/schema/rating.schema';
import { Order } from 'src/schema/order.schema';
import { Wallet_req } from 'src/schema/wallet_req.schema';
export declare class CustomerService {
    private readonly UserModel;
    private readonly RatingModel;
    private readonly Order;
    private readonly wallet_req;
    constructor(UserModel: Repository<User>, RatingModel: Repository<Rating>, Order: Repository<Order>, wallet_req: Repository<Wallet_req>);
    getData(id: number): Promise<{
        orderCount: number;
        walletAmount: number;
        ReviewRating: number;
        rating: number;
        id: number;
        image_url: string;
        name: string;
        dialing_code: number;
        remark: string;
        device_token: string;
        status: string;
        wallet_balance: number;
        address_one: string;
        address_two: string;
        phone_num: number;
        email: string;
        id_number: string;
        vehical_name: string;
        provider_profile: boolean;
        vehical_plat_num: string;
        custom_fields: string;
        reset_password_token: string;
        user_role: number;
        is_active: boolean;
        current_status: boolean;
        is_block: boolean;
        is_pr_block: boolean;
        block_date: Date;
        block_day: number;
        subscription_id: number;
        expiry_date: Date;
        createdAt: Date;
        updatedAt: Date;
        deletedAt: Date;
        walletRequests: Wallet_req[];
        categoryrequests: import("../../../schema/category_request.schema").CategoryRequest[];
        productrequests: import("../../../schema/product_request.schema").ProductRequest[];
    }>;
    getList(): Promise<User[]>;
    getAllPages(page: number, pageSize: number, search: string, is_active: any): Promise<{
        totalItems: number;
        data: any[];
        totalPages: number;
        currentPage: number;
    }>;
    getDatabyid(id: number): Promise<User>;
    createData(data: any): Promise<any>;
    updateData(id: number, updateData: Partial<User>): Promise<import("typeorm").UpdateResult>;
    deleteData(id: number): Promise<User>;
    bulkDeletedata(ids: number[]): Promise<number>;
    changePassword(id: number, currentPassword: string, newPassword: string, confirmPassword: string): Promise<User>;
}
