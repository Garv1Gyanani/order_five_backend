import { Repository } from 'typeorm';
import { User } from 'src/schema/user.schema';
import { User_document } from 'src/schema/user_document.schema';
import { Rating } from 'src/schema/rating.schema';
import { Subscription } from 'src/schema/subscription.schema';
export declare class ProviderService {
    private readonly UserModel;
    private readonly UserDocumentModel;
    private readonly RatingModel;
    private readonly SubscriptionModel;
    constructor(UserModel: Repository<User>, UserDocumentModel: Repository<User_document>, RatingModel: Repository<Rating>, SubscriptionModel: Repository<Subscription>);
    getUserData(id: number): Promise<{
        id: number;
        image_url: string;
        name: string;
        dialing_code: number;
        remark: string;
        device_token: string;
        status: string;
        wallet_balance: number;
        address_one: string;
        address_two: string;
        phone_num: number;
        email: string;
        id_number: string;
        vehical_name: string;
        provider_profile: boolean;
        vehical_plat_num: string;
        custom_fields: string;
        reset_password_token: string;
        user_role: number;
        is_active: boolean;
        current_status: boolean;
        is_block: boolean;
        is_pr_block: boolean;
        block_date: Date;
        block_day: number;
        subscription_id: number;
        expiry_date: Date;
        createdAt: Date;
        updatedAt: Date;
        deletedAt: Date;
        walletRequests: import("../../../schema/wallet_req.schema").Wallet_req[];
        categoryrequests: import("../../../schema/category_request.schema").CategoryRequest[];
        productrequests: import("../../../schema/product_request.schema").ProductRequest[];
    }>;
    getUserList(): Promise<User[]>;
    getAllPages(page?: number, pageSize?: number, search?: string): Promise<{
        totalItems: number;
        data: any[];
        totalPages: number;
        currentPage: number;
    }>;
    createUserData(data: any): Promise<any>;
    updateUserData(id: number, updateData: Partial<User>): Promise<import("typeorm").UpdateResult>;
    deleteUserData(id: number): Promise<User>;
    bulkDeletedata(ids: number[]): Promise<number>;
    createUserDocument(data: any): Promise<any>;
    GetUserDocument(user_id: any): Promise<any[]>;
    UpdateUserDocument(id: any, data: any): Promise<any>;
    changePassword(id: number, old_pass: string, new_pass: string, confirm_pass: string): Promise<User>;
}
